spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/ #
      database: ecarbon_gdsc_test

  security:
    oauth2:
      client:
        registration:
          google:
            client-id:
            client-secret:
            scope:
              - email
              - profile

  web:
    resources:
      static-locations: classpath:/static/

app:
  frontend:
    url: http://localhost:8080